# SCD2 Pipeline Review — Plan vs Current Setup + Phase A/B Parquet Intersection

**Date**: 2026-05-19
**Authored by**: parent agent (this chat session); claude-opus-4-7; context pressure medium-high
**Context**: B-344 RB-15 SCD2 corruption replay runbook authoring planning session (provisional plan adopted from cross-cohort reviewer `ab9ac2f21c7bf7866`; see plan + research artifact provenance notes). This review synthesizes the substrate alignment + decision tree drift + Phase A/B Parquet intersection findings independently verified against the actual code substrate at HEAD.
**Scope**: SCD2 substrate review + RB-15 plan compatibility check + Phase A/B Parquet proposal interaction analysis.

---

## §1. Provenance + scope boundary

This review was authored by parent agent at user-direction "Save this as an SCD2 review" 2026-05-19. The underlying analysis draws from:

- **Plan deliverable** at `docs/migration/RB15_SCD2_CORRUPTION_REPLAY_PLAN_2026-05-18.md` (🟡 PROVISIONAL — authored out-of-scope by cross-cohort reviewer agent `ab9ac2f21c7bf7866` 2026-05-19; provisionally adopted per pipeline-lead 2026-05-19; full skill chain to be applied at B-344 closure commit)
- **Research artifact** at `docs/migration/_research/scd2-corruption-recovery-rb15-2026-05-18.md` (🟡 PROVISIONAL — same authorship caveat; 12 primary-source URLs independently verifiable but not yet skill-gated)
- **Actual code substrate at HEAD** — independently grep'd / read by this chat agent 2026-05-19

Substantive findings below are **independently verified against the actual code substrate**; they do not depend on the provisional plan + research authorship caveat.

**Out-of-scope for this review**: SCD2 pipeline implementation updates being done in the parallel Claude session (Phase 2 R1 cohort: commits `4b3e5c9` + `4872581` + `28c8d25` per provenance notes). This review is procedural / analytical only.

---

## §2. Substrate inventory ✅ — all cited tools + modules exist

| Cited in plan | Actual state at HEAD | Notes |
|---|---|---|
| `tools/validate_scd2.py` | ✅ exists | SCD2-P1-* invariant checker |
| `tools/repair_scd2.py` | ✅ exists | R-6 targeted repair (V-4 duplicate-active class) |
| `tools/scd2_replay_smoke.py` | ✅ exists | Smoke-test only |
| `tools/scd2_replay_range_smoke.py` | ❌ MISSING | Production-grade range CLI does NOT exist; tracked as B-540 |
| `tools/diagnose_stage_bronze_gap.py` | ✅ exists (1,692 LOC) | DIAG-1 5-theory diagnostic |
| `tools/detect_scd2_config.py` | ✅ exists | SCD2 config autodetection |
| `tools/validate_cdc.py` | ✅ exists | CDC-side validator |
| `data_load/parquet_replay.py` | ✅ exists (694 LOC) | M2 `replay_parquet_snapshot()` |
| `data_load/parquet_registry_client.py` | ✅ exists | M3 `query_snapshot()` + 5-status state machine |
| `scd2/engine.py` | ✅ exists (**2,306 LOC**) | Public surface matches plan citations |

**Plan said `scd2/engine.py` is 2,206 LOC**; actual is **2,306 LOC** (minor arithmetic drift; not material).

---

## §3. SCD2 engine public surface ✅ — alignment with plan

All functions the plan references exist in `scd2/engine.py`:

| Function | Line | Purpose |
|---|---|---|
| `run_scd2()` | L330 | Full Bronze comparison (small tables) |
| `run_scd2_targeted()` | (~L450+) | PK-targeted (windowed/large tables) |
| `_cleanup_orphaned_inactive_rows()` | L413 | B-4 orphan cleanup at SCD2 entry |
| `_build_scd2_insert()` | (~L562) | UPDATE-close path constructions |
| `_execute_bronze_updates()` | L658 | UPDATE batches per `label_suffix` |
| `_activate_new_versions()` | L707 | PK-staging match per SCD2-P1-c/f |
| `_crash_test_harness_c7()` | L208 | Crash-injection at "after close_old" boundary |
| `_apply_source_verifier_or_block()` | L245 | CDC verifier defense-in-depth integration |

**3-step atomic SCD2 pattern (E-2) is implemented as plan describes**:
1. INSERT(Flag=0) — new versions held inactive
2. close-old — UPDATE prior Flag=1 rows to close them
3. activate-new — flip new INSERTs from Flag=0 to Flag=1 (atomic per SCD2-P1-c/f via PK-staging match)

The `_crash_test_harness_c7` boundary at "after_close_old" exposes the canonical SCD2 crash failure mode (zero-active transient window) and is wired for Tier 4 crash-injection testing.

---

## §4. 🔴 DIAG-1 theory naming drift (plan-vs-substrate; gap-check missed this)

**Plan §3 decision tree lists**:
- IN_FLIGHT_ORPHAN / **DUPLICATE_ACTIVE** / NEVER_INSERTED / ALL_CLOSED / RESURRECTED_AS_INACTIVE

**Actual `tools/diagnose_stage_bronze_gap.py` L177-181 constants**:
- T1: `IN_FLIGHT_ORPHAN`
- T2: **`DELETED_FROM_SOURCE`** (NOT DUPLICATE_ACTIVE)
- T3: `NEVER_INSERTED`
- T4: `ALL_CLOSED`
- T5: `RESURRECTED_AS_INACTIVE`

### Diagnosis

The plan **conflated two distinct diagnostics**:
- **DIAG-1** (`diagnose_stage_bronze_gap.py`) — produces 5 STAGE-BRONZE-GAP theories; does NOT produce DUPLICATE_ACTIVE
- **`validate_scd2.py`** — produces V-4 duplicate-active findings as a separate category

DUPLICATE_ACTIVE is a V-4 condition surfaced by `validate_scd2.py`, not by DIAG-1. The plan substituted DUPLICATE_ACTIVE in place of `DELETED_FROM_SOURCE` (T2).

### Required correction for RB-15

The corrected RB-15 decision tree should structure the triage as TWO sequential diagnostic steps with distinct routing:

```
STEP 0: validate_scd2.py — check ALL SCD2-P1-* invariants
        → V-4 duplicate-active detected → V-4 REPAIR branch (repair_scd2.py)
        → SCD2-P1-c sentinel drift → targeted repair branch
        → SCD2-P1-e orphan → B-4 cleanup branch
        → else proceed to STEP 1

STEP 1: diagnose_stage_bronze_gap.py — for PKs in Stage-Bronze gap
        → DIAG-1 routing on 5 ACTUAL theories:
          T1 IN_FLIGHT_ORPHAN      → B-4 cleanup
          T2 DELETED_FROM_SOURCE   → hard-close in Bronze; CDC verifier check
          T3 NEVER_INSERTED        → PK-scoped replay
          T4 ALL_CLOSED            → PK-scoped replay
          T5 RESURRECTED_AS_INACTIVE → PK-scoped replay (E-18)

STEP 2: If neither STEP 0 nor STEP 1 surfaces specific class
        → ETL_LOGIC_BUG path (full date-range sequential replay)
        → OR REGISTRY_CORRUPTED → escalate to RB-8
```

This is a material correction that **must be applied at the B-344 closure commit** (Phase 2 R2.12 deliverable), not deferred.

---

## §5. Phase A Parquet redesign × RB-15 intersection

### Phase A core change (per D115 + D116)

| Pre-Phase-A (current) | Phase A (in flight; parallel session) |
|---|---|
| Tokenize-then-Parquet | **Parquet-then-tokenize** (D115 reorder) |
| Parquet contains tokens (no plaintext PII at rest) | **Parquet contains plaintext PII at rest** during Phase A window |
| Bronze contains tokens | Bronze still contains tokens |
| No extraction-timestamp in Parquet | **Extraction timestamp in `key_value_metadata`** (D116) |

### Phase A intersection points with RB-15

1. **D115 reorder requires NEW re-tokenization step in RB-15 replay procedure**:
   - Replay reads PLAINTEXT PII from Parquet snapshot
   - Must invoke `pii_tokenizer.py` re-tokenization BEFORE Bronze write
   - Pre-Phase-A procedure had this step ABSENT (Parquet was already tokenized)

2. **D116 extraction-timestamp enables better snapshot-selection logic**:
   - Plan's "scan forward from confirmed-clean BusinessDate" should use `udm_extraction_timestamp_utc` from Parquet `key_value_metadata` (D116 R1) — more semantically meaningful than registry `CreatedAt`
   - Source-of-truth distinction: `CreatedAt` = registry insertion timestamp; `udm_extraction_timestamp_utc` = when source DB was queried
   - For "scan forward from clean state", the latter is authoritative

3. **SE1-SE10 source-exactness invariants enable NEW post-replay validation gate**:
   - Phase A guarantees Parquet bytes match source exactly
   - RB-15 can add: read back Parquet → compare bytes to Bronze tokens via reverse-tokenization
   - This is a NEW validation step Phase A makes possible

4. **B-383 `parquet_replay.py` tokenize-then-hash refactor** changes M2 signature:
   - `replay_parquet_snapshot()` signature evolution
   - RB-15 references M2; **must wait for B-383 to land before locking RB-15 body**

5. **B-381 auditd compensating control** (H drive Parquet directory):
   - Plaintext PII at rest during Phase A window → security control needed
   - D103 13-layer Layer 9 (auditd) extension
   - RB-15 pre-flight should reference auditd attestation

6. **B-375 wires extraction timestamps through orchestration**:
   - `parquet_writer.write_snapshot()` receives extraction timestamps
   - Required for D116 metadata population
   - RB-15 snapshot-selection logic depends on this

### Sequence dependencies for RB-15 lock

RB-15 v1.0 (Phase A scope) **must NOT lock before**:
- **B-383** `parquet_replay.py` tokenize-then-hash refactor lands (M2 signature stable)
- **B-375** extraction-timestamps wired through orchestration (D116 metadata populated)
- **B-381** auditd compensating control on H drive (R36 score-acceptable Phase A posture)
- **B-377** Polars Decimal128 → Parquet round-trip verified (SE-2 invariant)

These are Phase 2 R1 prereqs. The parallel session is building toward this.

---

## §6. Phase B PME × RB-15 intersection (deferred amendment path)

### Phase B core change

When PME (Parquet Modular Encryption) lands (legal counsel deps 4-16 weeks per B-389):
- Parquet PII columns are encrypted at rest (B-353-357)
- Plaintext-footer mode preserves `key_value_metadata` access (D116 R1-4)
- Column-key crypto-shredding enables CCPA-compliant column-level deletion

### Phase B intersection points with RB-15

1. **PME decrypt invocation in replay procedure**:
   - Encrypted PII columns require PME decryption during replay
   - Performance: 24-48hr for 3B-row replay per B-364 CRITICAL benchmark
   - RB-15 amendment needed: explicit decrypt step + perf warning

2. **Crypto-shredding STRUCTURALLY MITIGATES NB-B**:
   - If PME column-key shredded for CCPA-deleted SubjectIdentifier, replay cannot decrypt those rows (by design)
   - Post-Phase-B snapshots have CCPA compliance built-in
   - NO procedural CCPA filter needed for post-Phase-B snapshot replays

3. **RB-15 v1.1 amendment path** (when Phase B lands):
   - Add PME decrypt step to procedure
   - Add crypto-shred validation step (verify expected rows are unreadable)
   - Add 24-48hr executive-visibility escalation per B-364 perf characteristics
   - Document Phase A vs Phase B procedural differences

---

## §7. NB-B (CCPA-deleted-PK resurrection) era-specific risk reassessment

This is where the Phase A/B distinction matters most for compliance:

| Era | Parquet content | NB-B risk | Mitigation |
|---|---|---|---|
| Pre-Phase-A (current; in production) | Bronze tokens reference Vault | LOW | Structural: SP-2 returns `CCPA_DELETED` verdict; PII not resurrectable from Bronze tokens |
| **Phase A (NEAR-TERM)** | **Parquet has plaintext PII at rest** | **HIGH** | **Procedural: MANDATORY pre-replay CCPA filter step in RB-15** |
| Phase B (deferred 4-16wk) | PME-encrypted PII columns; column-key crypto-shredded for deleted subjects | LOW | Structural: encrypted PII unreadable post-shred |

**NB-B is a Phase-A-window-specific HIGH-risk concern** — exactly the era we're authoring RB-15 for.

### Procedural implementation (Phase A era)

Pre-flight Step (NEW): query `General.ops.CcpaDeletionLog` for SubjectIdentifiers deleted between snapshot's `udm_extraction_timestamp_utc` and current time → either:
- (a) exclude affected PKs from replay scope, OR
- (b) re-apply CCPA deletion post-replay via CCPA-deletion stored procedure (per CLAUDE.md SP discipline)

This step is **MANDATORY in Phase A** but **N/A in Phase B** (crypto-shred handles it structurally).

---

## §8. NB-A (`scd2_replay_range` CLI; now B-540) composes with B-375

The missing production-grade range CLI (tracked as B-540 per provenance notes) should leverage Phase A's extraction-timestamp wiring:

- B-375 wires extraction timestamps through orchestration → Parquet `key_value_metadata`
- B-540 range CLI should **accept `--start-extraction-timestamp` AND `--end-extraction-timestamp`** in addition to BusinessDate
- More authoritative for "when source data was current" than BusinessDate

This makes B-540 a Phase 2 R2.12 deliverable (sequence-dependent on B-375).

---

## §9. Edge case coverage validation

The plan's post-replay validation checklist correctly references real edge case identifiers:

| Validation check | Edge case ID | Status |
|---|---|---|
| Zero duplicate active rows | V-4 (Do-NOT rule) | ✅ canonical |
| Zero `Flag=1 AND UdmSourceEndDate != '2999-12-31'` | SCD2-P1-c | ✅ canonical |
| Zero `Flag=0 AND UdmEndDateTime IS NULL AND op IN ('U','R')` | SCD2-P1-e | ✅ canonical |
| E-18 resurrected PKs check | E-18 | ✅ canonical |
| E-5 pks_to_close dedup | E-5 | ✅ canonical |
| validate_scd2.py HEALTHY | SCD2-P1-a through P1-f | ✅ canonical |
| Source-exactness post-replay byte-comparison | SE-1 through SE-10 (Phase A NEW) | 🟡 not in plan; add at RB-15 authoring |

---

## §10. Recommendations (consolidated)

### Material corrections required at B-344 closure commit

1. **🔴 Fix DIAG-1 theory naming**: substitute `DUPLICATE_ACTIVE` → `DELETED_FROM_SOURCE` (T2) in decision tree. Restructure triage into TWO sequential steps (STEP 0 validate_scd2 + STEP 1 diagnose_stage_bronze_gap).
2. **🟡 Update B-344 BACKLOG entry** if not already done: cite RB-15 (not RB-13). Per provenance notes, this was changed at adoption commit 2026-05-19.
3. **🟡 Verify B-540 + B-541 are open** per provenance notes:
   - B-540: production-grade `scd2_replay_range` CLI
   - B-541: udm-cohort-review skill prompt strengthening (read-only audit clause)

### Phase A authoring discipline (RB-15 v1.0)

4. **Add NEW Step 2.5 to RB-15 procedure**: re-tokenize after Parquet read + before Bronze write (per D115 Phase A reorder)
5. **Update snapshot-selection logic** to use `udm_extraction_timestamp_utc` from Parquet `key_value_metadata` (D116) — more authoritative than registry `CreatedAt`
6. **Promote NB-B to MANDATORY Phase A pre-flight step**: query `CcpaDeletionLog` between snapshot's extraction-timestamp and current time; filter or re-delete
7. **Add SE-N source-exactness validation** as new post-replay validation gate
8. **Document sequence dependencies** in RB-15 §7 Approval Gates: cannot lock until B-375 + B-381 + B-383 + B-377 land
9. **Add Phase B amendment placeholder** in RB-15 §8 Cross-references for PME-era v1.1

### Plan-discipline corrections (provenance + cohort review)

10. **Re-verify research citations** at B-344 closure commit (research artifact is 🟡 PROVISIONAL per provenance notes; URLs independently verifiable but not yet skill-gated)
11. **Re-apply planning-session-startup discipline** at B-344 closure commit since plan was authored out-of-scope (§0 provenance table currently shows skills as-if proper protocol was followed; will be restarted at closure)
12. **Track B-541 forward-prevention**: udm-cohort-review skill prompt update to enforce read-only-audit clause (prevent future cross-cohort reviewer scope-expansion)

---

## §11. Substrate readiness verdict

**Code substrate**: ✅ FULLY READY for RB-15 implementation. Every function the plan invokes exists. 3-step atomic SCD2 (E-2) + idempotency-ledger (D15) + Parquet medallion (D2/D4) + DIAG-1 diagnostic + V-4 repair + `_cleanup_orphaned_inactive_rows()` are all built and locked.

**Procedural substrate**: ❌ MISSING — RB-15 placeholder at `05_RUNBOOKS.md` L1548-1554 needs full body. Plan provides starting structure; B-344 closure commit should apply corrections (§10) before locking body.

**Sequence dependencies**: 🟡 4 Phase 2 R1 prereqs (B-375 + B-377 + B-381 + B-383) must land before RB-15 v1.0 locks.

**CCPA compliance gap during Phase A window**: 🔴 HIGH risk — NB-B procedural mitigation is the critical RB-15 v1.0 addition. Without it, replaying pre-CCPA-deletion Parquet snapshots would resurrect plaintext PII the deletion erased.

---

## §12. Cross-references

- `docs/migration/RB15_SCD2_CORRUPTION_REPLAY_PLAN_2026-05-18.md` — 🟡 PROVISIONAL plan (authored out-of-scope by reviewer)
- `docs/migration/_research/scd2-corruption-recovery-rb15-2026-05-18.md` — 🟡 PROVISIONAL research artifact (same caveat)
- `docs/migration/05_RUNBOOKS.md` L1548-1554 — RB-15 placeholder (to be replaced at B-344 closure)
- `docs/migration/BACKLOG.md` — B-344 + B-540 + B-541 (per provenance notes)
- `docs/migration/03_DECISIONS.md` — D2 / D4 / D15 / D75 / D102 / D103 / D115 / D116
- `docs/migration/04_EDGE_CASES.md` — SCD2-P1-a through P1-f / SCD2-R2-a/b / SCD2-R4-R10.2 / DIAG-1 / LT-2 / LT-3 / E-2 / E-5 / E-18 / V-4 / SE-1 through SE-10
- `CLAUDE.md` Do-NOT rules SCD2-P1-* preservation + CDC_VERIFY_STRICT_ON_FAILURE + `_filter_null_pks()` + `_cleanup_orphaned_inactive_rows()`
- `scd2/engine.py` L208 (`_crash_test_harness_c7`) — crash-injection boundary
- `data_load/parquet_replay.py` — M2 `replay_parquet_snapshot()`
- `data_load/parquet_registry_client.py` — M3 5-status state machine
- `tools/diagnose_stage_bronze_gap.py` L177-181 — DIAG-1 canonical 5 theory constants

---

## §13. Status

**This review**: ⚫ COMPLETE — independently verified analysis of plan-vs-substrate alignment + DIAG-1 theory drift + Phase A/B intersection. Saved as procedural record for B-344 closure cohort reference.

**Next actions** (this chat):
- Pivot to B-494 session-compactor Phase 2 auto-trigger extension build (parallel work to today's SCD2 pipeline updates happening in parallel session)

**Awaits B-344 closure commit** (parallel session OR future this-chat work) to apply §10 corrections at RB-15 v1.0 authoring.
